public with sharing class DemoHelper {
    public DemoHelper() {
    system.debug('new mer');
    }
     //Method to add three number 
    public static Integer addThreeNumbers(Integer a, Integer b, Integer c){
        return a+b;
    }
}